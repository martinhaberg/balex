def filter_congested_bids(exchange_ranges, full_bid_list):
    """Establishes a reduced bid list based on availability in the network"""
    return None

def zonal_activation_optimization(ATC_list, imbalance_forecast_list, bid_list):
    """Identifies optimal balancing energy bid activation"""
    return None

def feasibility_check(network, initial_dispatch, selected_bids):
    """Detects whether optimized bid selection is feasible"""
    return None

def redispatch(network, initial_dispatch, selected_bids, full_bid_list):
    """Proposes adjustments to bid selection, could be done in several ways"""
    return None
